## 2026-03-31
- 对齐 `wpf-module-importer` skill 与当前 HEEG 项目主线，使其生成骨架遵循 `actions / elements / modules / cases / runner / tests / docs` 结构。
- 新增 `docs/project_delivery_overview.md`，用于再次演示与交付说明，系统说明项目结构、pytest 作用、自然语言到代码的转化链路、主要目录与文档职责。
- 合并并收敛冗余文档口径：重写 README、目录说明、映射说明和维护流程，删除重复的 `docs/project_tree.md`。
- 清理 `tmp/` 下的临时调试文件与无关缓存，减少演示交付时的噪音文件。
## 2026-03-31
- 修复弹窗关闭断言：关闭检测现在同时参考已绑定弹窗句柄和标题文本是否从当前应用窗口树中消失，避免设备设置关闭后因句柄绑定偏差阻断后续模块。
- 为弹窗页补充回归测试，覆盖“活动弹窗根节点解析”和“缓存句柄仍存在但标题已消失”的关闭场景。
## 2026-03-31
- 修复 dialog 页根节点绑定逻辑：设备设置等弹窗现在优先绑定活动弹窗本身，而不是回落到主窗口。
- 强化输入类控件查找：继续兼容 Edit/TextBox 回退，并配合弹窗根节点绑定，降低设备设置输入框定位失败概率。
- 正式用例 YAML 现在会拒绝重复键；例如同一个 `采样率` 写两次时，会在加载阶段直接报错，不再悄悄只保留最后一个值。

- 增强设备设置弹窗控件查找：对话框控件支持活动窗口兜底查找，并按最新确认修正 IP/设备名称输入控件类型。
- 修复设备设置中 IP/端口/设备名称输入控件按 Excel 原始 TextBox 类型建模，并同步当前 case 结构测试。
# Changelog

## 2026-03-31

- 支持“数据层可选、直接参数优先”的正式 case 编写规则。
- 修正 `TC_DEVICE_001` 多模块写法，提供正确的多模块 case 样板。
- 为 `患者姓名 / 性别 / 利手 / IP地址` 等业务文本补齐解析兼容。
- 元素仓库支持按 `aliases` 解析业务文本，例如“左手”“右利手”。
- README 与运行指南改成“直接参数优先”的说明，并补充了数据层使用示例。
- 更新 skill 说明，明确 `数据` 为可选层。


