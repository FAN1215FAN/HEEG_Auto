# 资产总表

## 文档定位

这份文档记录当前正式资产真源中的窗口、元素、断言资产清单。后续新增或删除正式资产时，必须同步更新这里。

## 一、资产统计快照

截至 2026-04-14，当前正式资产统计如下：

| 资产类型 | 文件数 | 正式目录 |
| --- | ---: | --- |
| 窗口资产 | 11 | `src/heeg_auto/assets/windows/` |
| 元素资产 | 7 | `src/heeg_auto/assets/elements/` |
| 断言资产 | 5 | `src/heeg_auto/assets/assertions/` |

## 二、窗口资产

### 聚合与推荐

| 文件 | 说明 |
| --- | --- |
| `recommended_windows.yaml` | 推荐窗口资产聚合入口 |

### 业务窗口

| 文件 | 当前用途 |
| --- | --- |
| `数字脑电采集记录软件窗口.yaml` | 主窗口与全局基础窗口 |
| `创建患者.yaml` | 新建患者窗口 |
| `创建检查.yaml` | 创建检查窗口 |
| `设备设置.yaml` | 设备设置窗口 |
| `历史回放窗口.yaml` | 历史回放主窗口 |
| `数据剪辑窗口.yaml` | 数据剪辑窗口 |
| `剪辑完成窗口.yaml` | 剪辑完成确认窗口 |
| `实时采集窗口.yaml` | 实时采集窗口 |
| `阻抗窗口.yaml` | 阻抗窗口 |
| `模拟器确认窗口.yaml` | 模拟器确认弹窗 |

## 三、元素资产

### 聚合与推荐

| 文件 | 说明 |
| --- | --- |
| `recommended_elements.yaml` | 推荐元素资产聚合入口 |

### 业务元素

| 文件 | 当前用途 |
| --- | --- |
| `数字脑电采集记录软件窗口.yaml` | 主窗口全局常用元素 |
| `创建患者.yaml` | 新建患者相关元素 |
| `设备设置.yaml` | 设备设置相关元素 |
| `历史回放窗口.yaml` | 历史回放相关元素 |
| `数据剪辑窗口.yaml` | 数据剪辑相关元素 |
| `剪辑完成窗口.yaml` | 剪辑完成相关元素 |

## 四、断言资产

### 聚合与推荐

| 文件 | 说明 |
| --- | --- |
| `recommended_assertions.yaml` | 推荐断言资产聚合入口 |

### 业务断言

| 文件 | 当前用途 |
| --- | --- |
| `创建患者.yaml` | 新建患者相关断言 |
| `设备设置.yaml` | 设备设置相关断言 |
| `实时采集与阻抗.yaml` | 实时采集与阻抗相关断言 |
| `历史回放剪辑.yaml` | 历史回放与数据剪辑相关断言 |

## 五、当前资产覆盖判断

### 覆盖较明确的区域

- 新建患者
- 设备设置
- 历史回放基础窗口
- 数据剪辑基础窗口
- 实时采集与阻抗基础窗口

### 历史回放剪辑专项当前已覆盖的关键资产

- 主窗口
- 历史回放窗口
- 数据剪辑窗口
- 剪辑完成窗口
- 历史回放剪辑相关命名断言
- 图形交互依赖的窗口级交互标定
- 基于 `剪辑开始时间 / 剪辑结束时间` 自动推导的时长范围断言能力

### 仍需继续补厚的区域

- 图形和波形类断言
- 复杂表格、树、弹窗类元素
- 历史回放和数据剪辑的细粒度断言
- 全局高频基础元素的命名统一

## 六、维护规则

1. 新增正式资产时，必须同步登记到这份总表。
2. 删除正式资产时，必须同步从这份总表移除，并说明替代关系。
3. `recommended_*.yaml` 只作为推荐聚合入口，不替代业务资产明细。
4. 如果业务资产只是临时试验，不得直接算入正式总表。

## 七、最近更新

### 2026-04-14

- 恢复这份文档的明细结构，不再只保留大类名称
- 根据当前真实 `assets/windows`、`assets/elements`、`assets/assertions` 目录重建清单
- 并入历史回放剪辑专题中关于关键资产范围的有效内容

### 2026-04-20

- 补充历史回放剪辑专项当前已覆盖的关键能力：基于 `剪辑开始时间 / 剪辑结束时间` 自动推导的时长范围断言。
